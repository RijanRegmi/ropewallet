{
  "name": "ropewallet-workspace",
  "version": "1.0.0",
  "description": "RopeWallet Workspace - Decoupled Frontend and Backend System",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "start": "concurrently -n \"BACKEND,FRONTEND\" -c \"cyan,magenta\" \"npm start --prefix backend\" \"npm start --prefix frontend\""
  },
  "dependencies": {
    "@vercel/node": "^5.1.10",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
